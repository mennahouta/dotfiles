# OS cruft
.DS_Store

# Backups created by install.sh
*.bak
*.bak.*

# Never commit secrets — these should never be in the repo, but guard anyway
**/*credentials*
**/*secret*
**/id_rsa
**/id_ed25519
**/*.pem
**/*.key
